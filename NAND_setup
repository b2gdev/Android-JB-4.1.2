HOW TO SET NAND BOOT VARIABLES
===================================

o Prepare a bootable SD card (Recommend to use "syscheck 1.1" image)
o Copy the contents of the “NandFS” folder to the 3rd partition (named “Data”) of the SD card
o Hit any key and stop the boot processes when the U-boot prompts “Hit any key to stop autoboot”
o Enter the following command

run cpall

